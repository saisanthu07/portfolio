# Development (Vite proxy handles /api requests — leave as is)
VITE_BACKEND_URL=

# Production — your EC2 backend URL or domain
# VITE_BACKEND_URL=http://your-ec2-public-ip:5001
# VITE_BACKEND_URL=https://api.yourdomain.com
